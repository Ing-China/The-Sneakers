import {StyleSheet} from 'react-native';
import {FontSizes, Padding} from '../../constants';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  buttonContainer: {},
  buttonLabel: {
    fontSize: FontSizes.large,
  },
  contentContainer: {
    padding: Padding.DEFAULT,
  },
});
